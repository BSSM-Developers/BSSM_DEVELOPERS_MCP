[project]
name = "bssm-dev-mcp"
version = "0.1.0"
description = "MCP server for bssm-dev proxy API"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
bssm-dev-mcp = "main:main"
